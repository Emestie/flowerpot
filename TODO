systems to create:

+100 weight for all

log system
    timestamp - added - id - title - ...
    maybe i can get removed WIs

show mine wis before others option
    (-20 weight)
add isMine option to WI itself

ability to hide certain ids from main list (until any changes on them)
favorites list (same as hide)
    mark them purple or some color too
    (-40 weight)

deferred list (+40 weight, gray)
permawatch list (shadow query?, show anytime if not empty, weight 99999), dont load query but load wis from list instead (mark pink)
add state to permawatch list

manage all lists in single view

settings page
    q to watch
    work items
    others (app, lang)

dont show same notif twice (store shown id and rev and check id)

change all 'save' to 'ok'

copy wi in clipboard