import store from "../store";

export default class Utils {
    // public static getPathWithCredentials() {
    //     let path = store.settings.tfsPath;
    //     let user = store.settings.tfsUser;
    //     let pwd = store.settings.tfsPwd;

    //     path = path.replace("://", `://${user}:${pwd}@`);
    //     return path;
    // }
}
